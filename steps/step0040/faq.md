### How can I be sure the PR checks on Jenkins server will not fail?

Run `make validate` on your host machine.

This command will do static analysis, syntax check, and tests. The build server runs similar commands.
